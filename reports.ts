import {getSupabase} from './supabase';
export async function allRows(table:string){const rows:Record<string,unknown>[]=[];for(let from=0;;from+=500){const {data,error}=await getSupabase().from(table).select('*').order(table==='seguimiento_amigos'?'punto_id':'id').range(from,from+499);if(error)throw new Error(`No se pudo consultar ${table}.`);rows.push(...data);if(data.length<500)return rows}}
export function downloadFile(name:string,content:string,type:string){const url=URL.createObjectURL(new Blob([content],{type}));const link=document.createElement('a');link.href=url;link.download=name;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
export function csv(rows:Record<string,unknown>[]){const headers=Array.from(new Set(rows.flatMap(row=>Object.keys(row))));const escape=(value:unknown)=>{let s=typeof value==='object'?JSON.stringify(value):String(value??'');if(/^[=+@\-\t\r]/.test(s))s="'"+s;return '"'+s.replace(/"/g,'""')+'"'};return '\ufeff'+[headers,...rows.map(row=>headers.map(h=>row[h]))].map(row=>row.map(escape).join(';')).join('\r\n')}
